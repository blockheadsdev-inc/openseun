class Token {
  final String walletTokenId;
  final String walletId;
  final String tokenId;
  final int price;
  final int balance;

  Token(this.walletTokenId, this.walletId, this.tokenId, this.price,
      this.balance);
}
